---
import { Icon } from "astro-icon";

interface Props {
  link: string;
  name: string;
}
const { link, name } = Astro.props;
---

<a href={link} target="_blank">
  <Icon name={name} class="h-16 w-16 text-white" />
</a>
