---
import { Icon } from "astro-icon";

interface Props {
  link: string;
  name: string;
}
const { link, name } = Astro.props;
---

<a href={link} target="_blank">
  <Icon name={name} class="h-20 w-20 p-2 text-white transition-all hover:text-orange-400" />
</a>
